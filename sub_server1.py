import socket
import threading

#class threadServer(threading.Thread):
    # def __init__(self, host, port):
    #     threading.Thread.__init__(self)
    #     self.host = host
    #     self.port = port
    #     self.controlX =